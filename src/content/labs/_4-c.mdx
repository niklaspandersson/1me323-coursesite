---
title: Bildspel
---

<aside class="first">

#### Syfte
* Att använda klasser för praktisk UI-utveckling.
* Att kapsla in bildspelslogik i en återanvändbar komponent.
* Att hantera tillstånd och användarinteraktion.
* Att arbeta med bilder och asynkron laddning.

</aside>

<main>
Nu ska vi använda allt vi lärt oss om klasser för att bygga en praktisk komponent - ett bildspel (slideshow/carousel). Detta är ett vanligt UI-mönster som passar perfekt för objektorienterad design.

### 0 - Förberedelser
Skapa en ny mapp `4c` i din `labs`-mapp med `index.html`, `main.js` och `slideshow.js`. Länka gärna in `global.css`.

Skapa också en `images`-mapp med 4-5 bilder, eller använd placeholder-bilder:
```
https://picsum.photos/800/400?random=1
https://picsum.photos/800/400?random=2
...
```

### 1 - Planera klassen
Innan vi börjar koda, tänk igenom vad bildspelet behöver:

**Tillstånd (data)**:
* Lista med bilder
* Vilken bild som visas just nu (index)
* Om bildspelet spelar automatiskt

**Beteende (metoder)**:
* Gå till nästa/föregående bild
* Gå till en specifik bild
* Starta/stoppa automatisk uppspelning
* Rendera till DOM

**Konfiguration**:
* Intervall för automatisk uppspelning
* Om det ska loopa
* Eventuellt: animeringar, bildtexter

### 2 - Grundstruktur
Skapa grundstrukturen för klassen i `slideshow.js`:

```javascript
export class Slideshow {
  #images;
  #currentIndex = 0;
  #container;

  constructor(images, containerSelector) {
    this.#images = images;
    this.#container = document.querySelector(containerSelector);

    if (!this.#container) {
      throw new Error(`Container "${containerSelector}" hittades inte`);
    }

    this.#render();
  }

  get currentIndex() {
    return this.#currentIndex;
  }

  get totalImages() {
    return this.#images.length;
  }

  #render() {
    // Implementera rendering
  }
}
```

### 3 - Implementera navigation
Lägg till metoder för att navigera mellan bilder.

```javascript
next() {
  this.#currentIndex = (this.#currentIndex + 1) % this.#images.length;
  this.#render();
}

previous() {
  this.#currentIndex = (this.#currentIndex - 1 + this.#images.length) % this.#images.length;
  this.#render();
}

goTo(index) {
  if (index < 0 || index >= this.#images.length) {
    throw new Error("Ogiltigt index");
  }
  this.#currentIndex = index;
  this.#render();
}
```

<details>
<summary>Förklaring av modulo-tricket</summary>

```javascript
// Om vi har 5 bilder (index 0-4)
// och är på index 4, vad är nästa?
(4 + 1) % 5 = 0  // Loopar tillbaka till början!

// Om vi är på index 0, vad är föregående?
(0 - 1 + 5) % 5 = 4  // Loopar till slutet!
```

Modulo (`%`) ger oss resten vid division, vilket ger automatisk looping.
</details>

### 4 - Rendera bildspelet
Implementera `#render()` för att visa bildspelet i DOM.

Ditt bildspel behöver:
* En container för bilden
* Navigeringsknappar (föregående/nästa)
* Eventuellt: indikatorer för vilken bild som visas

Skriv din egen HTML-struktur och CSS. Tänk på:
* Hur ska bilden visas? (`<img>` eller bakgrundsbild?)
* Var ska knapparna placeras?
* Hur visar du vilken bild användaren är på?

<details>
<summary>Tips för render</summary>

```javascript
#render() {
  const currentImage = this.#images[this.#currentIndex];

  this.#container.innerHTML = `
    <div class="slideshow">
      <button class="prev-btn">◀</button>
      <img src="${currentImage}" alt="Bild ${this.#currentIndex + 1}">
      <button class="next-btn">▶</button>
      <div class="indicators">
        ${this.#images.map((_, i) => `
          <span class="dot ${i === this.#currentIndex ? 'active' : ''}"
                data-index="${i}"></span>
        `).join('')}
      </div>
    </div>
  `;

  this.#bindEvents();
}
```
</details>

### 5 - Koppla händelser
Lägg till event listeners för knapparna.

```javascript
#bindEvents() {
  const prevBtn = this.#container.querySelector(".prev-btn");
  const nextBtn = this.#container.querySelector(".next-btn");

  prevBtn.addEventListener("click", () => this.previous());
  nextBtn.addEventListener("click", () => this.next());

  // Indikatorer
  const dots = this.#container.querySelectorAll(".dot");
  dots.forEach(dot => {
    dot.addEventListener("click", () => {
      const index = parseInt(dot.dataset.index);
      this.goTo(index);
    });
  });
}
```

### 6 - Automatisk uppspelning
Lägg till stöd för att bildspelet byter bild automatiskt.

```javascript
#intervalId = null;
#autoplayInterval;

constructor(images, containerSelector, options = {}) {
  this.#images = images;
  this.#container = document.querySelector(containerSelector);
  this.#autoplayInterval = options.interval || 3000;

  // ...
}

play() {
  if (this.#intervalId) return; // Redan igång

  this.#intervalId = setInterval(() => {
    this.next();
  }, this.#autoplayInterval);
}

pause() {
  if (this.#intervalId) {
    clearInterval(this.#intervalId);
    this.#intervalId = null;
  }
}

get isPlaying() {
  return this.#intervalId !== null;
}
```

Lägg till play/pause-knapp i din rendering och koppla den till metoderna.

### 7 - Använd klassen
I `main.js`, importera och använd din klass:

```javascript
import { Slideshow } from "./slideshow.js";

const images = [
  "https://picsum.photos/800/400?random=1",
  "https://picsum.photos/800/400?random=2",
  "https://picsum.photos/800/400?random=3",
  "https://picsum.photos/800/400?random=4"
];

const slideshow = new Slideshow(images, "#slideshow-container", {
  interval: 4000
});

// Starta automatisk uppspelning
slideshow.play();
```

### 8 - Utmaning: Förbättringar
Välj en eller flera förbättringar att implementera:

#### A - Tangentbordsnavigering
```javascript
#bindKeyboardEvents() {
  document.addEventListener("keydown", (e) => {
    if (e.key === "ArrowRight") this.next();
    if (e.key === "ArrowLeft") this.previous();
    if (e.key === " ") {
      e.preventDefault();
      this.isPlaying ? this.pause() : this.play();
    }
  });
}
```

#### B - Touch/swipe-stöd
Detektera swipe-gester för mobila enheter.

#### C - Lazy loading
Ladda bara bilder när de behövs för att spara bandbredd.

#### D - Bildtexter
Utöka datastrukturen så att varje bild kan ha en titel och beskrivning:
```javascript
const slides = [
  { src: "image1.jpg", title: "Solnedgång", description: "Vacker kväll" },
  // ...
];
```

#### E - Callbacks/Events
Låt användare av klassen lyssna på händelser:
```javascript
slideshow.onSlideChange((index, image) => {
  console.log(`Bytte till bild ${index}`);
});
```

<details>
<summary>Tips för callbacks</summary>

```javascript
#callbacks = {
  slideChange: []
};

onSlideChange(callback) {
  this.#callbacks.slideChange.push(callback);
}

#emitSlideChange() {
  this.#callbacks.slideChange.forEach(cb => {
    cb(this.#currentIndex, this.#images[this.#currentIndex]);
  });
}

// Anropa i next(), previous(), goTo()
```
</details>

**Reflektion**:
* Hur hjälper inkapsling till att göra komponenten återanvändbar?
* Vad är fördelarna med att ha all bildspelslogik i en klass istället för lösa funktioner?
* Hur skulle du göra komponenten mer konfigurerbar?
* Hur skiljer sig din implementation från bildspel du sett på webben?

</main>
